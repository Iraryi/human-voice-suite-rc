# Prompt bank: the situations LCCC does not contain

<!-- Generated by `npm run bank:report`. Statistics only: no source text. -->

> **Historical report, kept as published.** This was produced before the advice-permission gate was
> wired (Phase 28). Every row for `chat.unsolicited_advice` in it was measured with `requestKind`
> unpopulated — that is, with the rule's permission gate disabled, which is how every rate for that
> rule was produced until then. The rule now **abstains** wherever the permission is unknown, which
> is everywhere this harness ran, so a re-run of the tool that produced this file reports **zero**
> advice firings and shifts the counts below that depended on them. The published numbers are kept
> as they were measured; the current measurement is
> [`ADVICE_PERMISSION.md`](./ADVICE_PERMISSION.md), and the migration is in
> [`../../docs/phase-28-advice-permission.md`](../../docs/phase-28-advice-permission.md).
> Every other rule's rows here are unaffected.

```text
prompts original to this project, MIT
machine replies derived from them may be committed
```

## Why this exists

LCCC is one distribution: casual Weibo conversation. The situations that pull assistant behaviour out of a
model — being confided in, being asked to explain, being told something plainly wrong — are not most of what
that distribution contains. A rule that never fires on LCCC is not therefore a rule that never fires.

## What this is not

**Not a human/machine separation experiment.** There is no licence-clean human corpus for these situations,
and writing one by hand would compare the machine with my own idea of how a person answers. This is a
machine-behaviour stress test: it shows which situations pull which behaviours out of a model, and which of
those behaviours no rule covers.

| | |
| --- | --- |
| Prompts | 112 |
| Categories | 14 |
| Arms | `plain`, `default`, `post` — the same three as the paired control |
| Rules that fired anywhere | 5 |

## 1. Which situations pull which behaviour

Firings per category, per arm. | 14 categories x 3 arms |

| Category | Arm | Fires | Rules | Mean chars | Mean sentences | Assistant-shaped |
| --- | --- | --- | --- | --- | --- | --- |
| `venting` | `plain` | 0 | — | 34.9 | 3.00 | 0/8 |
| `venting` | `default` | 0 | — | 16.3 | 1.88 | 0/8 |
| `venting` | `post` | 0 | — | 30.8 | 2.88 | 0/8 |
| `asks-advice` | `plain` | 5 | `chat.over_completeness` | 124.8 | 5.00 | 0/8 |
| `asks-advice` | `default` | 0 | — | 90.9 | 3.13 | 0/8 |
| `asks-advice` | `post` | 6 | `chat.over_completeness` | 119.5 | 5.38 | 0/8 |
| `complains` | `plain` | 1 | `chat.unsolicited_advice` | 112.3 | 3.38 | 0/8 |
| `complains` | `default` | 0 | — | 24.5 | 1.00 | 0/8 |
| `complains` | `post` | 1 | `chat.unsolicited_advice` | 112.0 | 3.38 | 0/8 |
| `simple-question` | `plain` | 0 | — | 22.5 | 1.00 | 0/8 |
| `simple-question` | `default` | 0 | — | 19.5 | 1.13 | 0/8 |
| `simple-question` | `post` | 0 | — | 21.1 | 1.00 | 0/8 |
| `explain-concept` | `plain` | 1 | `chat.over_completeness` | 117.3 | 3.75 | 0/8 |
| `explain-concept` | `default` | 5 | `chat.over_completeness`, `chat.forced_positivity` | 170.8 | 5.50 | 1/8 |
| `explain-concept` | `post` | 1 | `chat.over_completeness` | 117.3 | 3.75 | 0/8 |
| `states-opinion` | `plain` | 0 | — | 62.9 | 3.00 | 0/8 |
| `states-opinion` | `default` | 1 | `chat.over_agreement` | 35.3 | 1.75 | 0/8 |
| `states-opinion` | `post` | 0 | — | 62.4 | 2.88 | 0/8 |
| `self-deprecation` | `plain` | 1 | `chat.forced_positivity` | 54.3 | 2.63 | 0/8 |
| `self-deprecation` | `default` | 0 | — | 7.8 | 1.00 | 0/8 |
| `self-deprecation` | `post` | 1 | `chat.forced_positivity` | 53.6 | 2.75 | 0/8 |
| `wrong-judgement` | `plain` | 0 | — | 97.3 | 3.50 | 0/8 |
| `wrong-judgement` | `default` | 0 | — | 49.1 | 1.50 | 0/8 |
| `wrong-judgement` | `post` | 0 | — | 96.6 | 3.50 | 0/8 |
| `asks-decision` | `plain` | 6 | `chat.over_completeness`, `chat.mirrors_user` | 118.4 | 4.63 | 0/8 |
| `asks-decision` | `default` | 0 | — | 28.8 | 1.88 | 0/8 |
| `asks-decision` | `post` | 0 | — | 112.5 | 3.88 | 0/8 |
| `shares-experience` | `plain` | 0 | — | 21.9 | 1.88 | 0/8 |
| `shares-experience` | `default` | 0 | — | 17.8 | 1.25 | 0/8 |
| `shares-experience` | `post` | 0 | — | 10.8 | 1.25 | 0/8 |
| `wants-chat` | `plain` | 1 | `chat.unsolicited_advice` | 26.6 | 2.13 | 0/8 |
| `wants-chat` | `default` | 1 | `chat.unsolicited_advice` | 15.3 | 1.50 | 0/8 |
| `wants-chat` | `post` | 1 | `chat.unsolicited_advice` | 24.6 | 2.00 | 0/8 |
| `multiple-requests` | `plain` | 0 | — | 54.4 | 2.50 | 0/8 |
| `multiple-requests` | `default` | 0 | — | 28.6 | 1.88 | 0/8 |
| `multiple-requests` | `post` | 0 | — | 52.4 | 2.63 | 0/8 |
| `open-question` | `plain` | 0 | — | 79.6 | 3.13 | 0/8 |
| `open-question` | `default` | 0 | — | 36.0 | 2.00 | 0/8 |
| `open-question` | `post` | 0 | — | 79.0 | 3.13 | 0/8 |
| `short-emotion` | `plain` | 0 | — | 16.4 | 1.50 | 0/8 |
| `short-emotion` | `default` | 0 | — | 8.5 | 1.38 | 0/8 |
| `short-emotion` | `post` | 0 | — | 16.3 | 1.50 | 0/8 |

## 2. Every rule, by category and arm

| Rule | Category | `plain` | `default` | `post` |
| --- | --- | --- | --- | --- |
| `chat.forced_positivity` | `explain-concept` | 0/8 | 1/8 | 0/8 |
| `chat.forced_positivity` | `self-deprecation` | 1/8 | 0/8 | 1/8 |
| `chat.mirrors_user` | `asks-decision` | 1/8 | 0/8 | 0/8 |
| `chat.over_agreement` | `states-opinion` | 0/8 | 1/8 | 0/8 |
| `chat.over_completeness` | `asks-advice` | 5/8 | 0/8 | 6/8 |
| `chat.over_completeness` | `explain-concept` | 1/8 | 4/8 | 1/8 |
| `chat.over_completeness` | `asks-decision` | 5/8 | 0/8 | 0/8 |
| `chat.unsolicited_advice` | `complains` | 1/8 | 0/8 | 1/8 |
| `chat.unsolicited_advice` | `wants-chat` | 1/8 | 1/8 | 1/8 |

## 3. Observations, not rules

Counted by reading all 336 answers once. One reader, one pass: these are observations and the counts are of
the sample, not rates about the world. Each is a behaviour the suite has no rule for, or has a rule for that
does not watch this form — the distinction that decides what to do about it. No text is quoted; the answers
are machine-written from prompts this project owns, but the report is counts either way.

| Shape | Count | Already watched by |
| --- | --- | --- |
| `fixer_reflex` — A complaint is answered with a numbered plan of action — who to call, what to say, which complaint channel to use. | `complains` `plain` 8/8, `default` 0/8 | `chat.unsolicited_advice` caught 1 of the 8. |
| `complaint_restatement` — The same complaint, said back in the complainer's voice with the feeling left in — joining rather than repairing. | `complains` `default` 8/8 | Nothing. It is the shape the contract produces instead. |
| `reframe_then_question` — Self-deprecation is answered with a diagnosis ("it is not that you are stupid, it is a missing prerequisite") and then an invitation to work on it. | `self-deprecation` `plain` 8/8 | `chat.forced_positivity` caught 1, and nothing watches the reframe. |
| `question_only_reply` — The same situation answered with a question and nothing else ("why do you say that"). The contract's shape here. | `self-deprecation` `default` 8/8 | Nothing, and on this reading it is the more human of the two. |
| `unrequested_addendum` — An explanation ends with one more fact nobody asked for, marked as an aside ("by the way, at dusk…"). | `explain-concept` `plain` 3/8, `wrong-judgement` `plain` 2/8 | Nothing. `chat.over_completeness` needs the ratio, and an aside does not move it. |
| `provenance_claim` — A correction arrives with a small history of the wrong belief — where it came from, what studies found. | `wrong-judgement` `plain` 3/8 | Nothing. |
| `numeric_scaffold` — Advice organised by count: three things to check, two questions to ask, the three parts that matter. | `asks-advice` `plain` 5/8, `asks-decision` `plain` 4/8 | Nothing directly; the enumeration is prose, not formatting. |
| `compact_verdict` — A decision answered in a clipped verdict with its reason attached ("hotpot." / "delete it. keeping it is for opening it again in a few days."). | `asks-decision` `default` 3/8 | Nothing. This is the contract over-correcting in the other direction. |

The largest of these is the one the suite is least equipped for. **A complaint is answered with a plan of
action in every unconstrained reply and none of the contract ones**, and the rule whose whole job that is
caught one of the eight, because it watches for a handful of phrases (`建议你`, `你可以试试`, `不妨`, `记得`,
`最好是`) and a plan of action does not have to use any of them. That is not a rule that is wrong. It is a
rule looking at one door in a wall.

The second largest is the same finding from the other side: the contract does not remove the pull to repair,
it changes what repair looks like — from fixing the problem to agreeing about it. Neither shape is watched,
and only one of them is assistant-like. **A rule cannot be written for this pair without deciding which of
the two is the tell**, which is a question about the product and not about the detector.

Two smaller ones are worth noting because they are the opposite of a coverage gap. `unrequested_addendum`
and `provenance_claim` are assistant behaviour by any reading — an aside nobody asked for, a small lecture
attached to a correction — and `chat.over_completeness` does not move for them because it counts sentences
against the user turn and these add one. A rule that watched the *addendum* rather than the length would
catch them, and that rule does not exist.

## 4. Limitations

1. **Machine only.** No human arm exists for these situations, and this file will not pretend otherwise.
2. **Eight prompts per category.** Enough to see which situations pull which behaviour, not enough for a
   rate with an error bar.
3. **One generator per arm.** A point, not a distribution.
4. **The prompts are mine.** They were written to be typical of each situation, which is not the same as
   being sampled from one.

## Reproducing it

```bash
npm run bank:prepare
# one generator per chunk file in .external-corpora/bank/chunks
npm run bank:prepare -- --only post
npm run bank:merge
npm run bank:report
```

Generated 2026-09-16T08:41:32.384Z from 336 answers.

